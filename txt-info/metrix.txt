Отдельное спасибо https://en.wikipedia.org/wiki/Box-drawing_characters
любезно предоставившим мне все эти чёрточи))))))))))))))

┌───────────────────────────────────────────────────────────────────────────────┐
│                        АРХИТЕКТУРА ОТПРАВКИ МЕТРИК                            │
└───────────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────────┐
│ 1. InfluxDB поток данных (для метрик k6)                                      │
└───────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐       K6_OUT=influxdb      ┌─────────────┐       HTTP API       ┌─────────────┐
│   k6 test   │ ─────────────────────────► │  k6 engine  │ ───────────────────► │  InfluxDB   │
│ (JavaScript)│   (переменная окружения)   │             │   POST /write        │   :8086/k6  │
└─────────────┘                            └─────────────┘   (Line Protocol)    └─────────────┘

Конкретно в данном проекте:
- Переменная окружения: K6_OUT=influxdb=http://influxdb:8086/k6
- Формат данных: Line Protocol (timestamp|measurement|tags|fields)
- Пример записи: http_reqs,url=https://horsepenis.com value=1 1639670400000000000

┌───────────────────────────────────────────────────────────────────────────────┐
│ 2. Prometheus поток данных (для системных метрик)                             │
└───────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐       scrape_configs       ┌─────────────┐       metrics      ┌─────────────┐
│ Prometheus  │ ◄───────────────────────── │node-exporter│ ◄───────────────── │  Host OS    │
│   :9090     │   (интервал: 15 секунд)    │   :9100     │   (/proc, /sys)    │ (CPU, RAM)  │
└─────────────┘                            └─────────────┘                    └─────────────┘

Конкретно в данном проекте:
- Конфигурация: prometheus/prometheus.yml
- Интервал: scrape_interval: 15s
- Targets: node-exporter:9100, influxdb:8086/metrics

┌───────────────────────────────────────────────────────────────────────────────┐
│ Примечания:                                                                   │
│ - k6 отправляет метрики в InfluxDB через HTTP API.                            │
│ - Prometheus собирает системные метрики с node-exporter и InfluxDB.           │
│ - Все компоненты взаимодействуют через Docker Network.                        │
└───────────────────────────────────────────────────────────────────────────────┘
